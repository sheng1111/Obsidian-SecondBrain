# Claude Code instructions

Read and follow `AGENTS.md` for the vault's persistent rules.

When the user asks to organize captures, the Inbox, Sources, Knowledge, work reviews, or the vault, invoke the `obsidian-inbox-organizer` project skill. Its canonical implementation is `.agents/skills/obsidian-inbox-organizer/`; read its required references before acting.

Never force-add personal vault notes or attachments to Git. Public project files must remain in English, while local vault content should follow the user's language and existing conventions.
